// Static HTML/CSS/JS site served directly from /public/index.html.
// React is intentionally not mounted. Keeping this file empty prevents
// any unintended rendering while CRA still injects its dev bundle.
